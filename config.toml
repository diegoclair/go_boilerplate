[app]
name = "go_boilerplate"
environment = "local"
port = "5000"
# Which edge is in front, most specific first. Empty uses the built-in list.
# Behind an AWS/GCP load balancer, where X-Forwarded-For is all there is, use
# echo's own XFF extractor with its trusted-range options instead.
client-ip-headers = []

  [app.auth]
  access-token-duration = "15m"
  refresh-token-duration = "24h"
  paseto-symmetric-key = "dFRpaeCkdLuKpv65vN7QDSGm5M4H6EWe"

[cache]
  [cache.redis]
  host = "cache" # redis container name
  port = 6379
  db = 0
  pass = "eYVX7EwVmmxKPCDmwMtyKVge8oLd2t81"
  default-expiration = "24h"
  
[db]
  [db.postgres]
  username = "root"
  password = "root"
  host = "db" # postgres container name
  port = 5432
  db-name = "go_boilerplate_db"
  sslmode = "disable"
  max-life-in-minutes = 1
  max-idle-connections = 5
  max-open-connections = 100

[log]
debug = true
log-to-file = false
path = "go_boilerplate.log"